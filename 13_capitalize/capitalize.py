def capitalize(phrase):
    """Capitalize first letter of first word of phrase.

        >>> capitalize('python')
        'Python'

        >>> capitalize('only first word')
        'Only first word'
    """
    return phrase[0].upper() + phrase[1:]

    # OMG this was return phrase.captialize()
    # I call foul, sir !!!!!!!